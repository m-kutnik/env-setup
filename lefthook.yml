# Lefthook git hooks configuration
# https://github.com/evilmartians/lefthook

pre-commit:
  commands:
    format:
      run: CI=1 mise run format
      stage_fixed: true
    gitleaks:
      run: mise exec -- gitleaks git --pre-commit --staged
