z4h bindkey undo Ctrl+Z # undo the last command line change
# z4h bindkey redo Ctrl+Shift+Z

# z4h bindkey '\e[A' history-beginning-search-backward
# z4h bindkey '\e[B' history-beginning-search-forward        # redo the last undone command line change
